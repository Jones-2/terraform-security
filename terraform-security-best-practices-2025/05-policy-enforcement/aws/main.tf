# Example: OPA policy usage (demo) - this does not automatically enforce in AWS but shows how OPA rego would look for blocking wide-open security groups.
# In production you could run OPA checks in CI or integrate with Gatekeeper-like tooling.
